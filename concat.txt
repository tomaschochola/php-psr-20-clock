<composer.json>
```
{
  "authors": [
    {
      "email": "tomaschochola@tomaschochola.cz",
      "homepage": "https://github.com/tomaschochola",
      "name": "Tomáš Chochola",
      "role": "Author"
    }
  ],
  "autoload": {
    "psr-4": {
      "TomasChochola\\Psr\\Clock\\": "./src"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Tests\\": "./tests"
    }
  },
  "config": {
    "allow-plugins": false,
    "audit": {
      "abandoned": "fail"
    },
    "platform-check": true,
    "process-timeout": 0,
    "sort-packages": true
  },
  "description": "tomaschochola/php-psr-20-clock",
  "funding": [
    {
      "type": "github",
      "url": "https://github.com/sponsors/tomaschochola"
    }
  ],
  "homepage": "https://github.com/tomaschochola/php-psr-20-clock",
  "keywords": [
    "tomaschochola",
    "TomasChochola",
    "Tomas Chochola",
    "Tomáš Chochola",
    "php-psr-20-clock"
  ],
  "license": "CC-BY-ND-4.0",
  "minimum-stability": "dev",
  "name": "tomaschochola/php-psr-20-clock",
  "prefer-stable": true,
  "readme": "./README.md",
  "repositories": [
    {
      "no-api": true,
      "type": "git",
      "url": "https://github.com/tomaschochola/php-tooling-phpstan-config.git"
    },
    {
      "no-api": true,
      "type": "git",
      "url": "https://github.com/tomaschochola/php-tooling-php-cs-fixer-config.git"
    }
  ],
  "require": {
    "php": "^8.5.0",
    "psr/clock": "^1.0",
    "psr/container": "^2.0"
  },
  "require-dev": {
    "ext-intl": "*",
    "ext-pcntl": "*",
    "ext-xdebug": "*",
    "ext-zip": "*",
    "phpunit/phpunit": "^12.5",
    "roave/security-advisories": "dev-latest",
    "tomaschochola/php-tooling-php-cs-fixer-config": "^1.0@dev",
    "tomaschochola/php-tooling-phpstan-config": "^1.0@dev"
  },
  "support": {
    "docs": "https://github.com/tomaschochola/php-psr-20-clock",
    "email": "tomaschochola@tomaschochola.cz",
    "issues": "https://github.com/tomaschochola/php-psr-20-clock/issues",
    "source": "https://github.com/tomaschochola/php-psr-20-clock.git"
  },
  "type": "library"
}
```
</composer.json>
<package.json>
```
{
  "author": {
    "email": "tomaschochola@tomaschochola.cz",
    "name": "Tomáš Chochola",
    "url": "https://github.com/tomaschochola"
  },
  "bugs": {
    "email": "tomaschochola@tomaschochola.cz",
    "url": "https://github.com/tomaschochola/php-psr-20-clock/issues"
  },
  "contributors": [
    {
      "email": "tomaschochola@tomaschochola.cz",
      "name": "Tomáš Chochola",
      "url": "https://github.com/tomaschochola"
    }
  ],
  "description": "@tomaschochola/php-psr-20-clock",
  "devDependencies": {
    "@tomaschochola/ts-tooling-eslint-config": "github:tomaschochola/ts-tooling-eslint-config#1.x",
    "@tomaschochola/ts-tooling-prettier-config": "github:tomaschochola/ts-tooling-prettier-config#1.x"
  },
  "funding": {
    "type": "github",
    "url": "https://github.com/sponsors/tomaschochola"
  },
  "homepage": "https://github.com/tomaschochola/php-psr-20-clock",
  "keywords": [
    "tomaschochola",
    "TomasChochola",
    "Tomas Chochola",
    "Tomáš Chochola",
    "php-psr-20-clock"
  ],
  "license": "CC-BY-ND-4.0",
  "name": "@tomaschochola/php-psr-20-clock",
  "repository": "github:tomaschochola/php-psr-20-clock",
  "sideEffects": false,
  "type": "module"
}
```
</package.json>
<src/NowClock.php>
```
<?php

/**
 * @author Tomáš Chochola <tomaschochola@tomaschochola.cz>
 * @copyright © 2026 Tomáš Chochola <tomaschochola@tomaschochola.cz>
 *
 * @license CC-BY-ND-4.0
 *
 * @see {@link https://creativecommons.org/licenses/by-nd/4.0/} License
 * @see {@link https://github.com/tomaschochola} GitHub Profile
 * @see {@link https://github.com/sponsors/tomaschochola} GitHub Sponsors
 */

declare(strict_types=1);

namespace TomasChochola\Psr\Clock;

use DateTimeImmutable;
use DateTimeZone;
use NoDiscard;
use Override;
use Psr\Clock\ClockInterface;

/**
 * @no-named-arguments
 */
readonly class NowClock implements ClockInterface
{
    #[NoDiscard]
    #[Override]
    public function now(): DateTimeImmutable
    {
        return new DateTimeImmutable('now', new DateTimeZone('UTC'));
    }
}
```
</src/NowClock.php>
<src/ClockTestingManifest.php>
```
<?php

/**
 * @author Tomáš Chochola <tomaschochola@tomaschochola.cz>
 * @copyright © 2026 Tomáš Chochola <tomaschochola@tomaschochola.cz>
 *
 * @license CC-BY-ND-4.0
 *
 * @see {@link https://creativecommons.org/licenses/by-nd/4.0/} License
 * @see {@link https://github.com/tomaschochola} GitHub Profile
 * @see {@link https://github.com/sponsors/tomaschochola} GitHub Sponsors
 */

declare(strict_types=1);

namespace TomasChochola\Psr\Clock;

use IteratorAggregate;
use NoDiscard;
use Override;
use Psr\Clock\ClockInterface;
use Traversable;

/**
 * @no-named-arguments
 *
 * @implements IteratorAggregate<mixed, mixed>
 */
readonly class ClockTestingManifest implements IteratorAggregate
{
    #[NoDiscard]
    #[Override]
    public function getIterator(): Traversable
    {
        yield ClockInterface::class => [FixedClockAssembler::class, 'assemble'];
    }
}
```
</src/ClockTestingManifest.php>
<src/ClockManifest.php>
```
<?php

/**
 * @author Tomáš Chochola <tomaschochola@tomaschochola.cz>
 * @copyright © 2026 Tomáš Chochola <tomaschochola@tomaschochola.cz>
 *
 * @license CC-BY-ND-4.0
 *
 * @see {@link https://creativecommons.org/licenses/by-nd/4.0/} License
 * @see {@link https://github.com/tomaschochola} GitHub Profile
 * @see {@link https://github.com/sponsors/tomaschochola} GitHub Sponsors
 */

declare(strict_types=1);

namespace TomasChochola\Psr\Clock;

use IteratorAggregate;
use NoDiscard;
use Override;
use Psr\Clock\ClockInterface;
use Traversable;

/**
 * @no-named-arguments
 *
 * @implements IteratorAggregate<mixed, mixed>
 */
readonly class ClockManifest implements IteratorAggregate
{
    #[NoDiscard]
    #[Override]
    public function getIterator(): Traversable
    {
        yield FixedClock::class => [FixedClockAssembler::class, 'assemble'];

        yield NowClock::class => [NowClockAssembler::class, 'assemble'];

        yield ClockInterface::class => [NowClockAssembler::class, 'assemble'];
    }
}
```
</src/ClockManifest.php>
<src/NowClockAssembler.php>
```
<?php

/**
 * @author Tomáš Chochola <tomaschochola@tomaschochola.cz>
 * @copyright © 2026 Tomáš Chochola <tomaschochola@tomaschochola.cz>
 *
 * @license CC-BY-ND-4.0
 *
 * @see {@link https://creativecommons.org/licenses/by-nd/4.0/} License
 * @see {@link https://github.com/tomaschochola} GitHub Profile
 * @see {@link https://github.com/sponsors/tomaschochola} GitHub Sponsors
 */

declare(strict_types=1);

namespace TomasChochola\Psr\Clock;

use NoDiscard;
use Psr\Container\ContainerInterface;

/**
 * @no-named-arguments
 */
readonly class NowClockAssembler
{
    #[NoDiscard]
    public static function assemble(ContainerInterface $container): NowClock
    {
        return new NowClock();
    }
}
```
</src/NowClockAssembler.php>
<src/FixedClock.php>
```
<?php

/**
 * @author Tomáš Chochola <tomaschochola@tomaschochola.cz>
 * @copyright © 2026 Tomáš Chochola <tomaschochola@tomaschochola.cz>
 *
 * @license CC-BY-ND-4.0
 *
 * @see {@link https://creativecommons.org/licenses/by-nd/4.0/} License
 * @see {@link https://github.com/tomaschochola} GitHub Profile
 * @see {@link https://github.com/sponsors/tomaschochola} GitHub Sponsors
 */

declare(strict_types=1);

namespace TomasChochola\Psr\Clock;

use DateTimeImmutable;
use DateTimeZone;
use NoDiscard;
use Override;
use Psr\Clock\ClockInterface;

/**
 * @no-named-arguments
 */
readonly class FixedClock implements ClockInterface
{
    private readonly DateTimeImmutable $now;

    public function __construct(DateTimeImmutable $now = new DateTimeImmutable('2000-01-01T00:00:00', new DateTimeZone('UTC')))
    {
        $this->now = $now;
    }

    #[NoDiscard]
    #[Override]
    public function now(): DateTimeImmutable
    {
        return $this->now;
    }
}
```
</src/FixedClock.php>
<src/FixedClockAssembler.php>
```
<?php

/**
 * @author Tomáš Chochola <tomaschochola@tomaschochola.cz>
 * @copyright © 2026 Tomáš Chochola <tomaschochola@tomaschochola.cz>
 *
 * @license CC-BY-ND-4.0
 *
 * @see {@link https://creativecommons.org/licenses/by-nd/4.0/} License
 * @see {@link https://github.com/tomaschochola} GitHub Profile
 * @see {@link https://github.com/sponsors/tomaschochola} GitHub Sponsors
 */

declare(strict_types=1);

namespace TomasChochola\Psr\Clock;

use NoDiscard;
use Psr\Container\ContainerInterface;

/**
 * @no-named-arguments
 */
readonly class FixedClockAssembler
{
    #[NoDiscard]
    public static function assemble(ContainerInterface $container): FixedClock
    {
        return new FixedClock();
    }
}
```
</src/FixedClockAssembler.php>
